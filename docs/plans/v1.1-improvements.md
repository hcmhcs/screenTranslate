# ScreenTranslate v1.1 개선 기획서

> 작성일: 2026-03-02
> 수정일: 2026-03-02 (HIG 준수 검토 반영)
> 기반: v1.0 (Phase 1~5 완료) 실사용 피드백

---

## 배경

v1.0 실사용 테스트에서 다음 개선 필요사항이 확인됨:
- 번역 결과를 다시 확인할 방법이 없음
- 언어팩 미설치 시 사용자가 상태를 알 수 없음
- 설정창/팝업 크기가 작아 내용이 잘림
- 원문과 번역문을 함께 비교할 수 없음
- 연속 번역 시 이전 요청이 에러로 표시됨

---

## 개선 항목

### 1. 번역 히스토리

**목적**: 이전 번역 결과를 다시 확인하고 활용할 수 있게 한다.

**요구사항**:
- 번역 완료/실패 기록을 시간순으로 저장
- 각 기록에 포함할 정보:
  - 타임스탬프
  - 원문 텍스트
  - 번역된 텍스트 (성공 시)
  - 실패 사유 (실패 시)
  - 소스 언어 → 타겟 언어
- 메뉴바에서 "히스토리" 항목으로 접근
- 히스토리 항목 클릭 시 번역문 복사
- 히스토리 전체 삭제 기능

**저장 방식**:
- v1.1: SwiftData 사용 (영구 저장, 최대 50건 표시)
- SwiftData를 사용하면 코드가 간단하고 향후 iCloud 동기화 가능

**데이터 모델** (SwiftData):
```swift
import SwiftData

@Model
final class TranslationRecord {
    var id: UUID                      // @Attribute(.unique) 불필요 - UUID는 이미 고유
    var timestamp: Date
    var sourceText: String
    var translatedText: String?       // nil이면 실패
    var failureReason: String?        // nil이면 성공
    var sourceLanguageCode: String    // "auto" 또는 언어 코드
    var targetLanguageCode: String

    init(
        sourceText: String,
        translatedText: String?,
        failureReason: String?,
        sourceLanguageCode: String,
        targetLanguageCode: String
    ) {
        self.id = UUID()
        self.timestamp = Date()
        self.sourceText = sourceText
        self.translatedText = translatedText
        self.failureReason = failureReason
        self.sourceLanguageCode = sourceLanguageCode
        self.targetLanguageCode = targetLanguageCode
    }
}
```

**⚠️ 아키텍처 주의사항**:
SwiftData `@Model`은 암시적으로 `@MainActor` 바인딩됩니다. 따라서 **Actor 기반 Manager는 사용하지 말고**, `@MainActor` 클래스를 사용해야 합니다:

```swift
// ❌ 잘못된 설계 (Actor + SwiftData = 안티패턴)
// actor TranslationHistoryManager { ... }

// ✅ 올바른 설계
@MainActor
final class TranslationHistoryManager: ObservableObject {
    static let shared = TranslationHistoryManager()
    private var modelContext: ModelContext?

    func configure(with context: ModelContext) {
        self.modelContext = context
    }

    func record(
        sourceText: String,
        translatedText: String?,
        failureReason: String?,
        sourceCode: String,
        targetCode: String
    ) throws {
        guard let context = modelContext else { return }
        let record = TranslationRecord(
            sourceText: sourceText,
            translatedText: translatedText,
            failureReason: failureReason,
            sourceLanguageCode: sourceCode,
            targetLanguageCode: targetCode
        )
        context.insert(record)
        try context.save()
    }

    func fetchRecent(limit: Int = 50) throws -> [TranslationRecord] {
        guard let context = modelContext else { return [] }
        let descriptor = FetchDescriptor<TranslationRecord>(
            sortBy: [SortDescriptor(\.timestamp, order: .reverse)]
        )
        descriptor.fetchLimit = limit
        return try context.fetch(descriptor)
    }

    func deleteAll() throws {
        guard let context = modelContext else { return }
        try context.delete(model: TranslationRecord.self)
        try context.save()
    }
}
```

**통합 지점** (기존 코드와의 연결):
```swift
// TranslationCoordinator.swift:89 - 완료 시 히스토리 기록
state = .completed(result)
Task { @MainActor in
    try? await TranslationHistoryManager.shared.record(
        sourceText: ocrResult.text,
        translatedText: translated,
        failureReason: nil,
        sourceCode: effectiveSource?.minimalIdentifier ?? "auto",
        targetCode: targetLanguage.minimalIdentifier
    )
}

// TranslationCoordinator.swift:98 - 실패 시에도 기록
state = .failed(error.localizedDescription)
Task { @MainActor in
    try? await TranslationHistoryManager.shared.record(
        sourceText: "...", // OCR 결과가 있을 수도, 없을 수도
        translatedText: nil,
        failureReason: error.localizedDescription,
        sourceCode: "...",
        targetCode: "..."
    )
}
```

**UI 설계** (HIG 준수):
```
┌─────────────────────────────────────────────┐
│  번역 히스토리                              │  ← Unified toolbar
├─────────────────────────────────────────────┤
│  오늘                                        │
│  ┌─────────────────────────────────────────┐│
│  │ 16:35  영어 → 한국어                     ││
│  │ "Registered Projects" → "등록된 프로젝트"││
│  │                               [복사]    ││
│  └─────────────────────────────────────────┘│
│  ┌─────────────────────────────────────────┐│
│  │ 16:32  영어 → 한국어                     ││
│  │ "Active Project:" → "활성 프로젝트:"     ││
│  │                               [복사]    ││
│  └─────────────────────────────────────────┘│
│  ┌─────────────────────────────────────────┐│
│  │ 16:30  영어 → 한국어            ⚠ 실패  ││
│  │ "Some text" → 언어팩 미설치              ││
│  └─────────────────────────────────────────┘│
│                                              │
│  ────────────────────────────────────────── │
│  [전체 삭제]                    [닫기]      │
└─────────────────────────────────────────────┘
```

**메뉴바 통합** (HIG 준수):
```
[앱 메뉴] [파일] [편집] [히스토리] [보기] [윈도우] [도움말]
                          │
                          ├─ 최근 번역 1    ⌘⇧1
                          ├─ 최근 번역 2    ⌘⇧2
                          ├─ ...
                          ├─ ─────────────
                          ├─ 모두 보기...   ⌘⇧H
                          └─ 전체 삭제
```

**키보드 단축키** (HIG 준수):
| 액션 | 단축키 |
|------|--------|
| 히스토리 윈도우 열기 | `Cmd+Shift+H` |
| 복사 (선택된 항목) | `Cmd+C` |
| 삭제 (선택된 항목) | `Delete` |
| 전체 삭제 | `Cmd+Delete` |
| 닫기 | `Esc` 또는 `Cmd+W` |

**HIG 준수 사항**:
- `WindowGroup` 사용, freely resizable
- `.windowToolbarStyle(.unified)` 적용
- 우클릭 컨텍스트 메뉴 지원
- 호버 상태 표시 (`.onHover`)
- 텍스트 영역 I-beam 커서

**구현 위치**:
- `TranslationRecord` 모델 (Core/History/)
- `TranslationHistoryManager` 클래스 (Core/History/)
- `HistoryView` (UI/History/)
- `MenuBarView`에 "히스토리" 메뉴 항목 추가
- `ScreenTranslateApp`에 `WindowGroup(id: "history")` 추가

---

### 2. 언어팩 상태 표시 및 다운로드

**목적**: 사용자가 언어팩 설치 상태를 한눈에 파악하고, 미설치 언어를 쉽게 다운로드할 수 있게 한다.

**요구사항**:
- 설정의 언어 피커에서 각 언어의 설치 상태 표시 (SF Symbols 사용)
  - `checkmark.circle.fill` (녹색): 다운로드됨 (즉시 사용 가능)
  - `arrow.down.circle` (주황색): 미다운로드 (다운로드 필요)
  - `ProgressView`: 다운로드 중
  - `xmark.circle` (회색): 지원하지 않음
- 미설치 언어를 선택하면 다운로드 확인 다이얼로그 표시
- 다운로드 진행률: 시스템 자체 UI 사용 (앱 커스텀 불가)

**Apple API**:
```swift
import Translation

// 언어 쌍의 지원 상태 확인
let availability = LanguageAvailability()
let status = await availability.status(
    from: Locale.Language(identifier: "en"),
    to: Locale.Language(identifier: "ko")
)
// status: .installed, .supported (다운로드 필요), .unsupported
```

**UI 설계** (HIG 준수 - SF Symbols):
```
┌─────────────────────────────────────┐
│ 원문 언어                            │
│ ┌─────────────────────────────────┐ │
│ │ 자동 감지                    ▾  │ │
│ └─────────────────────────────────┘ │
│                                      │
│ 번역 결과 언어                       │
│ ┌─────────────────────────────────┐ │
│ │ 한국어                    ✓    ▾│ │  ← checkmark.circle.fill (green)
│ │ ─────────────────────────────── │ │
│ │ English                      ✓  │ │
│ │ 日本語                       ↓  │ │  ← arrow.down.circle (orange)
│ │ 中文(简体)                   ↓  │ │
│ │ Français                     ✓  │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

**상태 아이콘 구현**:
```swift
@ViewBuilder
private func statusIcon(for code: String) -> some View {
    switch languageStatuses[code] {
    case .installed:
        Image(systemName: "checkmark.circle.fill")
            .foregroundStyle(.green)
    case .available:
        Image(systemName: "arrow.down.circle")
            .foregroundStyle(.orange)
    case .downloading:
        ProgressView()
            .controlSize(.small)
            .frame(width: 16, height: 16)
    case .unsupported, .none:
        Image(systemName: "xmark.circle")
            .foregroundStyle(.secondary)
    }
}
```

**다운로드 확인 다이얼로그** (HIG 준수):
```swift
// 시스템 Alert 스타일
Alert {
    Text("언어팩 다운로드")
    Text("\(languageName) 번역을 위해 언어팩이 필요합니다.\n시스템에서 다운로드를 진행합니다.")
}
actions: {
    Button("취소", role: .cancel) { }
    Button("다운로드") { startDownload() }
        .keyboardShortcut(.defaultAction)
}
```

**구현 위치**:
- `LanguagePackManager` 클래스 (Core/)
- `SettingsView` 수정 — 피커에 상태 아이콘 추가
- 다운로드 트리거는 `TranslationSession.Configuration`의 자동 다운로드 활용

---

### 3. 윈도우 크기 개선

**목적**: 설정창과 번역 팝업의 가독성을 높인다.

**현재 문제**:
- 설정창 (400×280): 항목이 많아지면 스크롤 발생
- 번역 팝업 (400×300 고정): 짧은 텍스트에는 과도하게 크고, 긴 텍스트에는 부족

**개선안**:

| 윈도우 | 현재 | 개선 |
|--------|------|------|
| 설정창 | 400×280 고정 | minWidth: 450, idealWidth: 500, maxWidth: 600<br>minHeight: 350, idealHeight: 400, maxHeight: 500 |
| 번역 팝업 | 400×300 고정 | 너비 320~480, 높이 콘텐츠 기반 (최소 120, 최대 400) |

**설정창 구현** (HIG 준수):
```swift
// Settings scene 사용 (Cmd+, 자동 연결)
Settings {
    TabView {
        GeneralSettingsView()
            .tabItem { Label("일반", systemImage: "gearshape") }

        LanguageSettingsView()
            .tabItem { Label("언어", systemImage: "globe") }

        ShortcutsSettingsView()
            .tabItem { Label("단축키", systemImage: "keyboard") }
    }
    .frame(
        minWidth: 450, idealWidth: 500, maxWidth: 600,
        minHeight: 350, idealHeight: 400, maxHeight: 500
    )
    .windowToolbarStyle(.unified)
}
```

**번역 팝업 크기 로직**:
```
- 로딩 상태 (인식 중/번역 중): 최소 크기 (320×120)
- 완료 상태: 텍스트 길이에 따라 유동적
  - 1~2줄: 320×160
  - 3~5줄: 400×250
  - 6줄 이상: 480×400 (스크롤)
- 에러 상태: 320×180
```

**팝업 크기 애니메이션** (HIG 준수):
```swift
func updateState(_ state: TranslationCoordinator.State, ...) {
    let newSize = calculateSize(for: state)

    // 부드러운 크기 전환
    withAnimation(.easeInOut(duration: 0.2)) {
        setContentSize(newSize)
    }

    hostingView?.rootView = makePopupView(state: state)
}

private func calculateSize(for state: TranslationCoordinator.State) -> NSSize {
    switch state {
    case .recognizing, .translating:
        return NSSize(width: 320, height: 120)
    case .completed(let result):
        let textHeight = estimateTextHeight(result.sourceText, result.text)
        let height = min(max(textHeight + 80, 160), 400)
        let width = textHeight > 200 ? 480 : (textHeight > 100 ? 400 : 320)
        return NSSize(width: width, height: height)
    case .failed:
        return NSSize(width: 320, height: 180)
    case .idle:
        return .zero
    }
}
```

**HIG 준수 사항**:
- 크기 변경 시 `withAnimation(.easeInOut(duration: 0.2))` 사용
- 텍스트 영역: I-beam 커서 (`NSCursor.iBeam`)
- `idealWidth`/`idealHeight` 파라미터 활용

**구현 위치**:
- `SettingsView` — frame 수정, TabView 구조로 변경
- `TranslationPopupWindow` — 동적 크기 계산 로직, 애니메이션 추가
- `TranslationPopupView` — idealWidth/maxHeight 조정

---

### 4. 원문 보기 기능

**목적**: 번역문과 원문을 나란히 비교할 수 있게 한다.

**요구사항**:
- 번역 완료 상태에서 "원문 보기" 토글 추가
- 토글 ON: 원문 (위) + 구분선 + 번역문 (아래)
- 토글 OFF: 번역문만 표시 (기본값)
- 원문도 텍스트 선택 + 복사 가능
- 키보드 단축키: `Cmd+O`

**UI 설계** (HIG 준수):
```
토글 OFF (기본):
┌─────────────────────────────────┐
│  등록된 프로젝트                 │
│  사용 가능한 도구 (비활성화)      │
│  사용 가능한 모드                 │
│                                  │
│  ☐ 원문 보기    [복사]  [닫기]   │  ← Checkbox 토글
└─────────────────────────────────┘

토글 ON:
┌─────────────────────────────────┐
│  원문                     영어   │  ← .secondary 색상
│  Registered Projects             │
│  Available Tools (Disabled)      │
│  ──────────────────────────────  │  ← Divider()
│  번역                     한국어  │
│  등록된 프로젝트                  │
│  사용 가능한 도구 (비활성화)       │
│                                  │
│  ☑ 원문 숨기기   [복사]  [닫기]  │
└─────────────────────────────────┘
```

**데이터 변경**:
```swift
// 기존
struct TranslationResult: Sendable {
    let text: String
    let lowConfidence: Bool
}

// 변경 - Locale.Language 사용 (타입 안전)
// ⚠️ Locale.Language는 macOS 15+에서 Sendable 준수
struct TranslationResult: Sendable {
    let sourceText: String           // 원문
    let translatedText: String       // 번역문 (명확한 네이밍)
    let lowConfidence: Bool
    let sourceLanguage: Locale.Language?  // 감지된 언어 (macOS 15+ Sendable)
}
```

**최소 지원 버전**: macOS 15.0+ (Locale.Language Sendable 준수 필요)

**UI 구현** (HIG 준수):
```swift
struct TranslationPopupView: View {
    @State private var showingOriginal = false

    var body: some View {
        VStack(alignment: .leading, spacing: 12) {
            // 번역문
            ScrollView {
                Text(result.translatedText)
                    .font(.body)
                    .foregroundStyle(.primary)
                    .textSelection(.enabled)
            }

            // 원문 (토글 시)
            if showingOriginal {
                Divider()

                VStack(alignment: .leading, spacing: 4) {
                    HStack {
                        Text("원문")
                            .font(.caption)
                            .foregroundStyle(.secondary)
                        Spacer()
                        Text(result.sourceLanguage?.localizedName ?? "")
                            .font(.caption)
                            .foregroundStyle(.secondary)
                    }
                    Text(result.sourceText)
                        .font(.body)
                        .foregroundStyle(.secondary)
                        .textSelection(.enabled)
                }
                .transition(.opacity.combined(with: .move(edge: .top)))
            }

            // 버튼
            HStack {
                Toggle("원문 보기", isOn: $showingOriginal)
                    .toggleStyle(.checkbox)
                    .keyboardShortcut("o", modifiers: .command)

                Spacer()

                Button("복사") { copy() }
                    .keyboardShortcut("c", modifiers: .command)

                Button("닫기") { close() }
                    .keyboardShortcut(.cancelAction)
            }
        }
        .animation(.easeInOut(duration: 0.2), value: showingOriginal)
    }
}
```

**키보드 단축키**:
| 액션 | 단축키 |
|------|--------|
| 원문 보기/숨기기 | `Cmd+O` |
| 복사 | `Cmd+C` |
| 닫기 | `Esc` |

**HIG 준수 사항**:
- Checkbox 토글 스타일 (상태 명확)
- 이차적 텍스트에 `.secondary` 색상
- 토글 시 `withAnimation` 사용
- `Divider()` 사용

**구현 위치**:
- `TranslationCoordinator.TranslationResult` — sourceText, sourceLanguage 필드 추가
- `TranslationPopupView` — 원문 보기 토글 UI
- `TranslationPopupWindow` — 토글 시 크기 조절 (애니메이션)

---

### 5. 연속 번역 충돌 해결

**목적**: 새로운 번역 요청 시 이전 요청이 에러가 아닌 정상 흐름으로 처리되게 한다.

**현재 문제**:
```
[로그] 에러: translationFailed("새 번역 요청으로 취소됨")
```
- `TranslationBridge.translate()`에서 기존 continuation을 에러로 resume
- 이로 인해 coordinator가 `.failed` 상태로 전환
- 사용자에게 "번역 실패" 로 보임

**해결 방안**: 순차 취소 (이전 작업 깨끗하게 정리 → 새 작업 시작)

**변경 사항**:

1. **TranslationBridge**: 기존 continuation 취소 시 `CancellationError`를 throw
   ```swift
   // 기존: TranslationError.translationFailed("새 번역 요청으로 취소됨")
   // 변경: CancellationError()
   if let existing = continuation {
       existing.resume(throwing: CancellationError())
       continuation = nil
   }
   ```

2. **TranslationCoordinator**: `CancellationError`는 이미 `.idle`로 조용히 처리
   ```swift
   } catch is CancellationError {
       state = .idle  // 이미 구현됨 — 에러 표시 없이 조용히 종료
   }
   ```

3. **AppOrchestrator.startTranslation()**: 이미 기존 작업을 취소하고 새 작업 시작하는 로직 있음. continuation 에러 타입만 변경하면 자연스럽게 동작.

**구현 위치**:
- `TranslationBridge.translate()` — resume 에러 타입 변경 (1줄)
- 나머지는 기존 코드가 이미 처리

---

## 구현 우선순위

| 순위 | 항목 | 난이도 | 영향도 | 비고 |
|------|------|--------|--------|------|
| 1 | 5. 연속 번역 충돌 | ⭐ | 🔴 높음 | 1줄 수정, 즉시 체감 |
| 2 | 3. 윈도우 크기 | ⭐⭐ | 🔴 높음 | 가독성 직결 |
| 3 | 4. 원문 보기 | ⭐⭐ | 🟡 중간 | 데이터 모델 변경 필요 |
| 4 | 1. 번역 히스토리 | ⭐⭐⭐ | 🟡 중간 | 새 윈도우/뷰 추가 |
| 5 | 2. 언어팩 상태 | ⭐⭐⭐ | 🟡 중간 | async API 연동 |

**권장 구현 순서**: 5 → 3 → 4 → 1 → 2

---

## 구현 Phase 계획

### Phase 6: 핫픽스 + 기반 개선
- 5번 연속 번역 충돌 수정 (TranslationBridge 1줄)
- 3번 윈도우 크기 개선
- 디버그 로그 → OSLog 변경
  ```swift
  import OSLog
  private let logger = Logger(subsystem: "com.app.screentranslate", category: "translation")
  logger.debug("OCR 결과: \(ocrResult.text)")
  ```

### Phase 7: 원문 보기 + 데이터 모델
- TranslationResult에 sourceText, sourceLanguage 추가
- 번역 팝업에 원문 보기 토글 (Checkbox + Cmd+O)
- 팝업 동적 크기 조절 (애니메이션)

### Phase 8: 번역 히스토리
- TranslationRecord SwiftData 모델
- TranslationHistoryManager (`@MainActor` 클래스 사용)
  - ⚠️ Actor + SwiftData는 안티패턴 (MainActor 격리 충돌)
- HistoryView / WindowGroup(id: "history")
  - ⚠️ MenuBarExtra 앱에서 WindowGroup 사용 시 Dock/Cmd+Tab에 노출되지 않을 수 있음
  - 필요시 `NSWindow` 직접 사용 검토 (TranslationPopupWindow 패턴)
- 메뉴바 "히스토리" 메뉴 추가 (Cmd+Shift+H)
- TranslationCoordinator에 히스토리 기록 통합

### Phase 9: 언어팩 관리
- LanguageAvailability API 연동
- 설정 피커 상태 표시 (SF Symbols)
- 다운로드 확인 다이얼로그

---

## HIG 준수 체크리스트

### Menu Bar
- [ ] 메뉴바에 "히스토리" 메뉴 추가 (Edit와 View 사이)
- [ ] 히스토리 메뉴: 최근 5개 표시 + "모두 보기..."
- [ ] 모든 메뉴 항목에 키보드 단축키

### Windows
- [ ] 히스토리 윈도우: `WindowGroup` 사용, resizable
- [ ] 설정 윈도우: `Settings` scene 사용, TabView 구조
- [ ] 팝업: 크기 변경 시 애니메이션 적용

### Keyboard
- [ ] 원문 보기: `Cmd+O`
- [ ] 복사: `Cmd+C`
- [ ] 닫기: `Esc`
- [ ] 히스토리: `Cmd+Shift+H`
- [ ] 전체 삭제: `Cmd+Delete`

### Visual Design
- [ ] 이모지 대신 SF Symbols 사용
- [ ] 시스템 semantic 색상 사용 (`.green`, `.orange`, `.secondary`)
- [ ] Dark Mode 지원 (semantic colors 사용)
- [ ] 애니메이션: `.easeInOut(duration: 0.2)`

### Pointer
- [ ] 텍스트 영역: I-beam 커서
- [ ] 히스토리 항목: 우클릭 컨텍스트 메뉴
- [ ] 호버 상태 표시

### Concurrency (Swift 6)
- [ ] TranslationHistoryManager: `@MainActor` 클래스 사용 (Actor ❌)
- [ ] TranslationRecord: SwiftData @Model (자동 MainActor)
- [ ] TranslationResult: Sendable 준수 (macOS 15+ 필요)
- [ ] CancellationError로 깔끔한 취소 처리

---

## 아키텍처 검토 결과 (2026-03-02)

**종합 평가: 3.5/5** - 잘 구조화되었으나 Swift 6 동시성 준수에 개선 필요

### 발견된 이슈 및 해결방안

| 이슈 | 심각도 | 해결방안 |
|------|--------|----------|
| Actor + SwiftData 안티패턴 | 🔴 높음 | `@MainActor` 클래스로 변경 |
| @Attribute(.unique) on UUID | 🟡 중간 | 제거 (UUID는 이미 고유) |
| 히스토리 통합 지점 미명시 | 🟡 중간 | TranslationCoordinator에 후킹 위치 명시 |
| Locale.Language Sendable | 🟡 중간 | macOS 15+ 최소 지원 명시 |
| WindowGroup 라이프사이클 | 🟢 낮음 | NSWindow 대안 검토 가능성 명시 |

### 참조 파일
- `TranslationCoordinator.swift:89-98` - 히스토리 기록 통합 지점
- `TranslationBridge.swift:34-36` - CancellationError 수정 대상
- `AppOrchestrator.swift:72-123` - 전체 파이프라인 흐름
- `ScreenTranslateApp.swift:12-21` - WindowGroup 배치 위치
